#include "collision-manager.h"

CollisionManager::CollisionManager() {
    //Collision Manager Class has been created.
}

void CollisionManager::CheckCollisions() {
    //check for collisions
    // Two kinds of collidable objects: static, dynamic
    // Dynamic objects need to check against all nearby static and dynamic objects for collisions
    // Do not have to check static objects against static objects

    // Parse through grid of collidable objects
    // If come across dynamic object, if any nearby objects exists
    // If nearby objects exists, check for collision
    // return true


    //******************************
    
    // C
}